

// JavaScript for responsiveness (optional)
window.addEventListener('resize', function () {
    if (window.innerWidth > 600) {
        location.reload(); // Reload page if viewport width becomes greater than 600px
    }
});

